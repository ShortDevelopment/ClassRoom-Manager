<UserControl
    x:Class="ClassRoom_Manager.UI.Controls.ToolPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClassRoom_Manager.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Button x:Name="MainButton" BorderThickness="2" Style="{StaticResource ButtonRevealStyle}" BorderBrush="{ThemeResource SystemControlBackgroundListMediumRevealBorderBrush}" Background="Transparent" Margin="10">
        <Grid Height="100" Width="250" Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <FontIcon Glyph="&#xE8B9;" FontSize="40" Grid.Column="0" x:Name="FontIcon" />
            <StackPanel VerticalAlignment="Center" Grid.Column="1">
                <TextBlock Text="Title" Style="{StaticResource SubtitleTextBlockStyle}" x:Name="TitleTextBlock" />
                <TextBlock Text="Description" Style="{StaticResource BodyTextBlockStyle}" x:Name="DescriptionTextBlock" />
            </StackPanel>
        </Grid>
    </Button>
    
</UserControl>
